\documentclass[10pt,twocolumn]{article}
\usepackage[a4paper, margin=1.8cm]{geometry}
\usepackage{booktabs,graphicx,parskip,hyperref}
\title{Rules that Bet: a Calibrated, Hypothesis-Driven CHIA Loop\\for Cross-SoC Cache-Hierarchy Design}
\author{Mateo Buscarons \and (PhD teammate) \and (PhD teammate)}
\date{}
\begin{document}
\maketitle

\begin{abstract}
% What: LLM architect + surrogate + explicit playbook of rules with a bet ledger.
% Mechanism: pre-registered quantitative bets settled by the simulator; disagreement-based
% experiment selection; losing rules re-scoped, never deleted.
% Result (fill): simulations-to-target on unseen SoC C vs 6 baseline arms; Brier of rules vs LLM vs surrogate.
% Contribution to CHIA: simulator-agnostic ledger/forecaster/selector blocks; config-space ChampSim node; gs:// traces.
\end{abstract}

\section{Introduction}
% Cache hierarchy tuning restarts from zero per SoC; architects carry rules. LLM loops exist (ArchAgent, MicroEvo,
% gem5 Co-Pilot, CHIA's own evolutionary prefetcher case study) but the LLM grades its own reasoning and nothing
% transfers except a surrogate. Our question: does explicit, *calibrated* architectural knowledge transfer better?

\section{The loop}
\subsection{Forecasters and bets}
% surrogate (GP), playbook rules (condition, claim, example; win/loss + Brier), LLM hypotheses.
% Every forecaster states P(objective >= T) before the run; T is the midpoint of the dispute.
\subsection{Disagreement-based experiment selection}
\subsection{Re-scoping, not deleting}
\subsection{Distillation and transfer}
\subsection{CHIA implementation}
% Fig: chia viz-profile task graph. Nodes: build_from_config, simulate (ChampSimNode), AnalystNode (Vertex).
% Resources: champsim_build / champsim / vertex_creds. Cache keyed by config name.

\section{Experimental setup}
% ChampSim; SoC profiles A/B/C (Table); Tier-A space (81 configs) with hard area budget per SoC; traces mcf, gcc, lbm;
% dense reference sweep = ground truth (729 sims); 10M instr + 5M warmup.
% Arms: random, surrogate, surrogate\_pooled, textbook, rules, analyst, full. 3 seeds. Metric: sims to 98\% of optimum.

\section{Results}
% Fig 1: best-so-far curves on C. Fig 2: simulations-to-target per arm. Fig 3: running Brier per forecaster class.
% Fig 4: reliability diagram. Table: the playbook (rules, records) incl. one re-scoped rule before/after.
% Cost: LLM \$ and simulation count.

\section{Discussion and limitations}
% Small Tier-A space; placeholder SoC profiles; single-core; textbook arm as contamination defense.

\section{Artifacts}
% Open-source loop; CHIA PRs: gs:// trace resolver, config-space ChampSim node, ledger blocks.

\end{document}
