# CHIA Hackathon — Rules that Bet (knowledge that survives an unseen chip *and* an unseen workload class)

A3 workshop hackathon (agentic-arch.org). Deliverable: 4-page paper + open-sourced CHIA loop with reproducible results; "reusable CHIA blocks upstreamed to mainline" is an explicit track. Judged by the A3 program committee, run by the six organizers: Gonzalez and Jain (Google, ArchAgent authors), Karandikar (UC Berkeley, CHIA PI), Yazdanbakhsh (Google DeepMind, ArchGym: "all optimizers tie under tuned hyperparameters"), Huang (NVIDIA, DOSA), Skarlatos (CMU). They reward novelty and quality of the agentic architecture, matched-budget baselines, seeds, realizability (they flagged ChampSim's lack of area/power feedback themselves), and reusable blocks + datasets. Not raw IPC.

**The judges' own paper, and the open problem we answer.** ArchAgent (Gupta, Jain, Gonzalez et al., arXiv 2602.22425, Feb 2026; four of the six organizers are authors) builds on AlphaEvolve to have an agent write new cache-replacement policies directly inside ChampSim; it reports +5.3% IPC on multi-core Google traces in two days and +0.9% on single-core SPEC06 in 18 days, validates short searches with long runs (cascaded evaluation), and admits workloads by baseline LLC MPKI > 1. Its follow-up, ArchAgent v2 (arXiv 2608.09874), repeats the exercise on the Data Prefetching Championship. Three things it does not do, stated by its authors: it starts every search from scratch (no transfer between chips or workloads), its agent's hypotheses stay as unscored free text, and it names "overfitting versus specialization, extrapolation from representative workloads" as an open community problem. Our plan is built to answer exactly that third point, with the first two as supporting claims. Because their margins are 1-3% and come from designing new policies while we search a knob space, we never compare on IPC.

## Start here (next session)

1. **Current stand:** the v7 mechanism below is the only design; it is fully coded and offline-verified (see "Code status" under Repo layout). Nothing is committed since the second plan review; run `git status` and commit when the user says so.
2. **Next actions, in dependency order (third plan review, decisions 25-35):** build the fixes A (pooled distillation, degenerate-condition rejection, two-stage re-scoping, fixed-threshold scoring, per-workload bets, `bo_pooled_x`), the previous-chip's-best start, the memory system (cases, beliefs, recipe, scoreboard) and the `memory` / `handoff` arms, cell 2b's traces; pre-register P5-P8; then the pilot (cell 2, memory from A/B) and cell 2b; the 300-design reference per cell. Every launch needs the user's OK with cost and time.
3. **The proof of success:** cell 2 and cell 2b with the `memory` arm against `llm_direct` (P5, P6) and `handoff` (P7), `rules_v2` against `bo` (P8), scored at 90/95/99% of a 300-design uniform reference per cell. Gate G-language stays as the free check of the descriptor language.
4. **Open points the user owns:** chip D's profile and budget and the three GAP mixes (placeholders in `loop/socs.py` and `loop/run.py`); the CACTI technology node (decision 20); the miss-count GP target is settled: not adopted (decision 21); cell 2b's third trace (cc) still to be fetched; which GAP traces exactly form cell 2b.
5. **First cell-2 result (overnight chain `v1`, two seeds, 24 designs, all seven arms, playbook of 10 rules distilled from A+B on the five-workload suite; report `results_vm/experiment_v1_gap.json`, logs `results_vm/v1_*.log`).** Designs to 90% of the best design found in the run (0.6426), median over the two seeds, and final best: `llm_direct` **6** (0.641), `rules` 12.5 (0.616), `full` 13.5 (0.620), `textbook` 14 (0.609), `bo` 14.5 (0.623), `random` >24 (0.584), `bo_pooled` >24 (0.546, flat from design 3 in both seeds). Calibration on shared dispute bets: surrogate Brier 0.225 (69% wins), rules 0.278 (35%), analyst 0.244; `llm_direct` on its own within-5% forecasts 0.181 (76%). **Read:** P1 held hard (the SPEC-pooled GP collapsed and never recovered); rules beat `bo_pooled` and `random`, tie `textbook` and cold `bo`, and lose to the plain agent, which found va_ampm_lite + next_line L1D + wider L2 by round 3; P4 fails as written. Rules' bets lost because the *magnitudes* learned on A/B over-promise on GAP (RULE-001 claims +29.8%, GAP pair +13.6%) while the *signs* and the *firing* transferred (gate G-language: prefetcher rules right on all three workloads, chip A's capacity rule fires only on `bfs.urand`; chip B's capacity rule fired wrongly from a degenerate "< 0.49" condition because B's budget leaves movable LLC MPKI near zero for every training workload). 14 mechanical re-scopes; 11 claim tests, 20 stall scans and 10 replies over 48 rounds; no failed jobs; LLM cost 0.55 USD for the whole night. Two seeds: a direction, not a verdict. **Decision pending (user): what to change before the full cells - see the morning report options (rules as directions with bounded shifts; a calibration round on a new class; the held-out reference so 95/99% mean something; whether the easy prefetcher gain hides the comparison).**
6. **Pilot v4 (Sep 10 morning; 5 arms, 2 seeds, 8 rounds = 16 designs, 5M/10M, start = chip B's best design fitted to C, worth 0.545 on the GAP suite; playbook and memory `v2` pooled over A+B).** Best found 0.6419 (va_ampm_lite + L1D next-line + LLC 4096x16 + SHIP + no LLC prefetcher, several L2 shapes). Designs to 90/95/99% of it, median of 2 seeds: `llm_direct` 10.5 / 10.5 / 10.5 (both seeds reach 0.642), `memory` 13 / 13 / 13 (both seeds reach 0.642), `handoff` >16 (0.596, 0.623), `rules` >16 (0.594, 0.598; stuck at the start until stall scans), `bo` >16 (0.560, 0.609). Memory's cards placed **zero** bets (every card's claim was the start's own value or had no measured sibling) and its recipe sent it into the SPP basin first, as its SPEC-learned step 1 says; it still reached the best design by design 13 through its own reasoning (dropping the LLC prefetcher next to va_ampm_lite). Rules lost all 12 sign bets: EI's early far-away picks (ip_stride, tiny L2) were bad designs and the rules' siblings were GP-estimated, not measured. **Fidelity ladder** ruled out shorter pilots (see roadmap phase 1). v5 (memory + llm_direct, seeds 2-5) followed to get six seeds of the one comparison that matters. Reports: `results_vm/experiment_v4_gap.json` (+ v5).
7. **Last launched, finished (VM TERMINATED at ~03:40 on Sep 10; every step exit 0; the GAP fidelity tables and `v1_fidelity_gap.log` are still only on the VM disk - starting the VM for ~5 min to fetch and merge them needs the user's OK):** the overnight chain `bash cluster/chain.sh v1` on VM `champsim-1` (steps: smoke through CHIA, learn set on A/B over the five training workloads, distill + G-language, cell `gap` with two seeds through CHIA, fidelity check at 50M/50M; the held-out reference is opt-in with `RUN_HELDOUT=1` and was not run). Logs: `~/hackathon/results/v1_chain.log` (step timeline) and `results/v1_<step>.log`; outputs `results/experiment_v1_playbook.json`, `_pool.json`, `experiment_v1_gap.json`, `results/chia_profiles/`. The VM shuts itself down when the chain ends. **Next session: fetch `~/hackathon/results` from the VM, `python -m loop.collect merge <fetched dir>` to union the tables, read `v1_learn.log` (gate G-language) and `python -m loop.summarize results/experiment_v1_gap.json`, then decide on the held-out reference and the full cells.**
8. **Rules of engagement:** no dates or deadlines anywhere; launches need explicit OK; the Mac may run simulations overnight; code stays lean, one mechanism, no version archaeology; domain decisions are surfaced with options and left to the user.

## The mechanism (v7)

One sentence: **rules whose conditions are written on a workload's measurable habits - profiled once from its trace, never its identity - and whose claims are settled by the simulator on old chips, still fire correctly and still cut the search on a workload class and a chip they have never seen.**

This is a claim about the **rule language**, not about a learned model. Keep the two apart in every sentence we write: our own offline test shows a learned *surrogate* does **not** transfer to a held-out program (and the literature says it needs 7-30 training programs). A rule that says "footprint over LLC size > 4 and stride regularity < 0.2 -> SPP loses to next_line" references measurable habits, so it can be checked on a program nobody has ever profiled. That distinction is the paper.

- **Workload profile, once per trace, no simulator** (`loop/trace_profile.py`): working set, stride regularity, temporal locality, LRU miss-ratio curve (footprint theory, Xiang et al. ASPLOS 2013). Chip-independent by construction.
- **Conditions are chip-independent and program-agnostic**: profile descriptors read against the target chip's geometry (`champsim_problem.chip_descriptors`). The chip's own measured speed is never a condition. Thresholds fitted from per-workload evidence (v5.1).
- **Claims are measured, never stated**: controlled pair, |effect| >= 1%, sign matches the LLM's stated direction; rejections kept in `rejected_rules`. Losing rules are re-scoped, not deleted. Brier-scored ledger for every forecaster (surrogate, rules, analyst).
- **Rules in selection: soft, bounded categorical priors.** A speaking rule shifts the GP mean for its categorical value by at most (measured effect x credibility), decaying as real observations accumulate. **Never a filter.** Rules bet on every design they speak about. Dedicated one-knob claim tests only when EI brought no improvement last round (fixes v5.1's claim-test flood).
- **The LLM has two jobs, both ablatable:** (i) write conditions and rules from the measured one-knob effects; (ii) right of reply when EI stalls. The LLM never judges its own claims.
- **Multi-core mix descriptors**: on a chip where N cores share the LLC, a workload is a mix of N programs. Capacity descriptors **sum** across the mix (N cores sharing an LLC collectively need the sum of their working sets, so "footprint / LLC size" keeps its meaning); rate descriptors (accesses per kilo-instruction, stride regularity, locality) are **access-weighted averages**. Reuses the mean/max machinery `make_suite_problem` already has.
- **Objective:** suite geomean of per-workload IPC under a hard area cap. On a multi-core chip the cap must count **one L2 per core and one shared LLC**, which makes private capacity N times more expensive than shared - a real interaction the rules can be tested on.
- **Guardrails:** rebuild per config, budget re-check, fresh-build re-run of the final best design, skeptic pass that tries to falsify the accepted hypothesis.

**Demoted by the headline change, deliberately** (say so if you disagree): the CPI-stack mean function (failed G1, see below - it survives as an offline finding and a paper section, not as the surrogate's mean); the `physics` arm (it would just be a worse GP); the LLM term proposer (it existed to feed the stack); CACTI area/energy (stretch only - the KB area proxy with correct per-core accounting already carries the interesting constraint).

## Decisions taken in the first plan review (user)

1. **Headline: "knowledge survives a workload class it never saw."** Rules learned on SPEC-class programs and chips A/B are tested on graph-analytics workloads (GAP) on unseen chips. Chosen because ArchAgent - written by four of the six judges - names "overfitting versus specialization, extrapolation from representative workloads" as an open community problem, and because margins in this field are 1-3% and shrinking, so methodology is the currency, not IPC.
2. Rules in selection: **categorical priors, soft and bounded**, as above. Not filters, not bets-only.
3. **Fidelity check** (baseline + top-20 cached + ~5 random designs at 50M warmup / 50M sim, ~1 h wall on 32 vCPUs, a few USD). Every downstream number depends on whether big-LLC ranks hold under a real warmup.
4. `textbook` arm stays (LLM prior knowledge alone, no LLM at test time): answers "isn't the LLM's prior doing the work?" and gives the thesis figure: stated rules' Brier vs measured rules' Brier.
5. Fixed reference for Tier C SPEC suite: **0.7491** (verified: best suite geomean of the 447 C designs measured on all 4 workloads; chip C baseline 0.5990, 25.1% headroom). Each new cell needs its own reference, collected the same way.
6. Freeze one A/B warm-start pool for `bo_pooled`, identical across cells and seeds (today the pool is that run's own learn history, so the baseline inherited the treatment's choices).
7. **The mechanism freezes once every cell's smoke has passed through CHIA**; after that only measurement and writing. There is no calendar: the user works on it when they can and runs it when they can; phases are ordered by dependency and gated, not dated.
8. Spending: each launch needs the user's explicit OK with cost and time; VM stopped when idle.
9. **Workload hygiene, and a validated admission gate** (`python -m loop.offline admit`, zero cost, from the trace profile alone). ArchAgent's criterion (baseline LLC MPKI > 1) is necessary but not sufficient - it admits workloads whose misses are all compulsory. Ours: a workload is admitted for **capacity** only if, across the capacities the search space can actually buy (LLC read at 576-4608 KB on chip C), the miss-ratio curve moves at least **1 MPKI**. Validated against the Tier A full factorial: the gate's two ADMITs are exactly the two workloads with non-zero measured capacity variance, its two REJECTs exactly the two with ~zero.

| trace | MPKI@min | movable | unfixable (compulsory) | measured capacity variance | verdict |
|---|---|---|---|---|---|
| mcf | 23.5 | **12.6** | 10.7 | 0.46 | ADMIT |
| omnetpp | 11.0 | **2.6** | 8.3 | 0.86 | ADMIT |
| lbm | 45.2 | 0.6 | 42.7 | 0.01 | reject on capacity; **keep as the prefetcher workload** (l2_prefetcher owns 0.96 of its variance) |
| xalancbmk | 1.3 | -0.1 | 1.1 | - | **drop** (confirmed) |
| gcc | 69.5 | -0.7 | **69.5** | - | **reject** |

`xalancbmk` is dropped as planned. **`gcc` is rejected too, for the opposite reason:** its 43 MB footprint over a 10M-instruction window makes 100% of its 70 MPKI compulsory, and its curve is flat at 0.187 from 16 KB to 32 MB - no cache on any chip changes its miss count. (It has the highest stride regularity of any trace at 0.79, so it would be a strong *prefetcher* workload, but we already have lbm and the geomean would be swamped by a workload no capacity decision can move.) (Closed by decision 10: the remaining candidates were screened with this gate and fotonik3d and cam4 joined the training suite.)

10. **Workload admission gate, two channels** (`python -m loop.offline admit`, free, from trace profiles alone). 15 traces screened Sep 8. The **capacity** channel (miss-ratio curve moves >= 1 MPKI across the 576-4608 KB the space can buy) is *validated*: its verdicts match the measured chip C variance shares. The **policy** channel is a miss floor (>= 5 MPKI) and deliberately does **not** claim which policy - stride regularity is reported but not used to decide, because mcf's is 0.157 and the L2 prefetcher still owns 0.46 of its variance (SPP learns repeated irregular paths a stride count cannot see).

| trace | footprint | MPKI | movable | stride | measured cap.var / pf.var | admits as |
|---|---|---|---|---|---|---|
| `bfs.urand-36B` | 22.4 MB | 87.5 | **33.6** | 0.267 | - | capacity + policy |
| `bfs.urand-60B` | 22.5 MB | 82.2 | **33.1** | 0.241 | - | capacity + policy |
| `mcf` | 7.5 MB | 23.5 | **12.6** | 0.157 | 0.46 / 0.46 | capacity + policy |
| `omnetpp` | 5.6 MB | 11.0 | **2.6** | 0.013 | 0.86 / 0.00 | capacity + policy |
| `lbm` | 26.7 MB | 45.2 | 0.6 | 0.387 | 0.01 / 0.96 | policy |
| `pr.urand-129B` | 14.2 MB | 22.7 | 0.00 | **1.000** | - | policy (pure sequential scan) |
| `bfs.kron-128B` | 10.7 MB | 17.1 | 0.01 | 0.519 | - | policy |
| `pr.road-28B` / `pr.web-16B` / `bfs.road-99B` | 4-10 MB | 6.9-16.2 | <= 0.20 | 0.63-0.98 | - | policy |
| `gcc` | 43.4 MB | 69.5 | -0.7 | 0.792 | - | policy (100% compulsory: curve flat 16 KB - 32 MB) |
| `xalancbmk` | 0.9 MB | 1.3 | -0.1 | 0.475 | - | **REJECT** |
| `xz` / `deepsjeng` / `leela` / `perlbench` | 0.1-1.6 MB | 0.1-3.0 | <= 0.42 | - | - | **REJECT** (fit inside L2) |

**Two hypotheses of mine that the data killed, recorded so they are not retried.** (i) *The simulation window is the lever* - false: `bfs.kron` is capacity-flat at 10M, 30M and 45M alike, because its footprint grows as fast as the window. (ii) *The graph input decides* - false: `bfs.urand` is capacity-bound while `pr.urand` on the same graph is a perfectly sequential scan (stride 1.000). Among six GAP (kernel, graph) pairs only BFS-on-urand is capacity-sensitive, but it is so at both SimPoints tested. **Capacity sensitivity must be screened per trace; benchmark and input labels do not predict it.**

**18 SPEC traces screened, 5 capacity-sensitive**: mcf 12.6, cam4 5.0, fotonik3d 3.9, pop2 2.7, omnetpp 2.6. Everything else fails both channels: `xz` 1.6 MB, `roms` 839 KB, `x264` 333 KB, `deepsjeng` 238 KB, `imagick` 223 KB, `leela` 172 KB, `nab` 471 KB, `cactuBSSN` 2.0 MB (movable -0.52), `xalancbmk` 862 KB, and `exchange2` at **18 KB**, which never leaves the L1. That is our own measurement of ArchAgent's claim that SPEC is unrepresentative - most of it does not touch memory hard enough to test a cache hierarchy at all.

**Final suites (user, Sep 8).** Training: **mcf, omnetpp, lbm, fotonik3d, cam4** - four capacity channels plus lbm for prefetching, footprints spanning 2.8-26.7 MB. Held-out: **bfs.urand-36B, pr.urand-129B, bfs.kron-128B**. Both suites need their own reference collected (the 0.7491 reference was the old 4-workload SPEC suite including xalancbmk and is now superseded).

**Descriptor coverage** (`python -m loop.offline coverage`) - the rigor table, and the reason the five-workload suite was worth +3 USD. 12 of 15 descriptor cells **interpolate**, including footprint and access rate for all three held-out workloads, which are the descriptors capacity conditions are written on:

| descriptor | training range | held-out position |
|---|---|---|
| footprint | 2.8 - 26.7 MB | all three inside (10.7 / 14.2 / 22.4 MB) |
| accesses per kinstr | 219 - 704 | all three inside (227 / 316 / 364) |
| stride regularity | 0.013 - 0.844 | `bfs.urand` 0.267 and `bfs.kron` 0.519 inside; **`pr.urand` 1.000 extrapolates above** |
| local reuse | 0.830 - 0.973 | `pr.urand` and `bfs.kron` inside; **`bfs.urand` 0.703 extrapolates below** |
| write fraction | 0.091 - 0.412 | `bfs.urand` and `pr.urand` inside; **`bfs.kron` 0.008 extrapolates below** |

The three extrapolations are named in the paper with their direction: `pr.urand` is *more* stride-regular than anything trained on (the safe direction for a prefetcher rule), `bfs.urand` has *less* local reuse (so replacement rules extrapolate), and `bfs.kron` is nearly read-only (so any write-conditioned rule extrapolates).

**Practical note:** a GAP kernel's zip is 10 GB but holds 31 separate traces of 24-630 MB. Read its zip64 central directory from the last 256 KB, then byte-range the one member wanted (`fetch_any.py` pattern). One trace costs ~150-600 MB, not 10 GB.

11. **Held-out workload class: chosen for DIVERSITY OF BINDING CONSTRAINT, not for headroom** (user, Sep 8). GAP ChampSim traces from Zenodo record 20043527 (BSC/UPC/TAMU, May 2026; cite the IISWC 2020 replacement-policy paper). The suite is `bfs.urand-36B` (capacity-only: movable 33.6, stride 0.267), `pr.urand-129B` (prefetcher-only: movable 0.00, stride 1.000) and `bfs.kron-128B` (mixed: movable 0.01, stride 0.519).

Selecting on headroom would have invited the obvious objection - "you chose the workloads where your rules could win" - so instead **the gate becomes a prediction instrument, not a selection filter**: for every rule and every held-out workload we pre-register whether the rule *should* fire, from the descriptors alone. A capacity-flat workload is then a **positive** test: a well-written capacity rule must stay silent when the descriptors say the footprint is far beyond any buyable cache. If it fires and loses, the descriptor language is wrong and we report that. **Gate G-headroom: PASSED, Sep 8, 40 designs x 3 held-out workloads on chip C, 33 min of VM time, 0.83 USD.** The suite has far more headroom than the SPEC suite: baseline geomean 0.2629, best of 40 designs 0.6110, **+132%** (against SPEC's +25.1%). Per-workload spread 64.9% / 197.8% / 178.0%.

**And the gate's pre-registered prediction held, quantitatively** - this is the first simulator-settled evidence for the descriptor language, which is the headline claim:

| single knob change | bfs.urand | pr.urand | bfs.kron |
|---|---|---|---|
| `llc_sets` 2048 -> 4096 (double the LLC) | **+10.5%** | **-0.7%** | **-1.2%** |
| `llc_replacement` lru -> ship | **+11.5%** | +0.5% | +0.8% |
| `l2_prefetcher` no -> va_ampm_lite | +16.5% | +155.4% | +132.7% |
| `l2_prefetcher` no -> spp_dev | +13.6% | +186.5% | +151.5% |
| `l2_sets`, `l2_ways`, `l1d_sets` changes | -1.2% .. +1.1% | -0.7% .. +0.6% | -1.3% .. +0.8% |

The gate said, from the traces alone with no simulator: capacity moves `bfs.urand` (movable 33.6 MPKI) and not `pr.urand` (0.00) or `bfs.kron` (0.01). The simulator agrees exactly - doubling the LLC is +10.5% on the one it flagged and **negative** on the two it did not (a bigger LLC is slower and buys nothing). Predicted for free, confirmed for 0.83 USD.

**Two further findings from the same sweep.** (i) **Replacement policy finally matters:** `ship` is +11.5% on `bfs.urand`, against 0.2-11% variance share on SPEC where the knob was nearly inert. That is exactly the claim the GAP trace set is published for (IISWC 2020) and it means the playbook can at last learn replacement rules. (ii) `bfs.urand` is the balanced workload - capacity +10.5%, replacement +11.5%, prefetcher +16.5% all comparable - which makes it the best single test of the rule language we have.

**A risk this exposes, needing a decision.** Turning on *any* L2 prefetcher buys +90% to +186% on two of the three held-out workloads, so every arm including `random` will capture most of the headroom within a design or two, compressing the differences between arms. The chip C baseline has no prefetcher at all (`BASELINE_KNOBS`), which is realistic as "the untouched chip" but makes the held-out search trivially easy at the start. **Decided (user, Sep 8): keep the identical no-prefetcher baseline in every cell** - it is what "the untouched chip" means, and every arm shares the head start. Arms are separated by **designs-to-90/95/99% of each cell's own reference**, never by raw headroom. Expect the best-so-far curves to sit on top of each other for the first design or two in the held-out cell, and say so in the caption rather than hiding it. **Provisional held-out reference: 0.6110** (best of only 40 designs; a real reference needs broader coverage - see the roadmap).

Also non-obvious and worth a rule: the best design of the 40 drops `l2_mshr` from 64 to **16** while running an aggressive prefetcher - fewer outstanding misses alongside heavy prefetching, presumably less queueing and pollution.
12. **Multi-core is an extra cell, not the backbone** (assessment delegated to Claude, Sep 8): a 4-core shared-LLC chip is the most realistic setting and matches the CRC-2 configs ArchAgent uses, but it costs ~4x per design, and the only way to afford making the whole experiment multi-core is dropping from 5 seeds to 2-3. Yazdanbakhsh will read variance across seeds first, so that trade is the one this committee punishes. Main experiment stays single-core at 5 seeds; one added cell runs `rules` vs `bo_pooled` at 3 seeds on the 4-core chip. Two risks stated in the paper: a mix has no single footprint (hence decision on mix descriptors above), and footprint theory predicts a *dedicated* cache's miss ratio, so the miss-ratio curve is only an approximation on a shared LLC.
13. **Chips are not made structurally different.** The block-size trick was rejected: the miss-ratio curve is measured in 64-byte blocks, so per-chip block sizes would destroy the profile's chip-independence, which is a founding principle. Verified that the chip axis is nonetheless non-trivial: A's best design ranks 314th of 447 on C, B's best 181st, and C's best does not even fit A's or B's area budget. Stated limitation: **our chips differ in provisioning (core width, MSHRs, DRAM, area budget, core count on chip D), not in hierarchy shape.**
14. **We pre-register our own experimental predictions** before the paid run and Brier-score ourselves on them: the three arm-level predictions in the paid-run section, plus one should-fire/should-stay-silent prediction per rule per held-out workload (decision 11).

## Decisions taken in the second plan review (user; expert consultation pending)

15. **CHIA is the runner, not an afterthought.** `run_chia.py` must run the real smoke cell on the VM with CHIA's profiler on before any paid cell, and every paid cell runs through it. Engineering note: `experiment.py` fans arms out with a process pool, and each Ray-dispatched arm would need its own Ray connection, so each arm becomes a Ray task itself. Once the CHIA smoke passes, `run.py` is retired (two runners = two code paths in the paper). **Why:** the judges built CHIA and the deliverable is "your CHIA loop and the results it produced"; results from a different runner are not the loop's results.
16. **`llm_direct` arm added: the plain agent (AgentDSE-style).** Each round the LLM picks the round's designs from the history table, the search space and the workload descriptors alone: no GP, no rules, no priors. Same model and temperature as the analyst; infeasible or repeated proposals are rejected and re-asked; it states a predicted IPC per pick so it enters the Brier ledger like every forecaster. **Why:** without it nothing answers "isn't a plain chatbot just as good?" - AgentDSE's own anonymisation ablation showed the LLM is a capable black-box optimiser by itself, and the judges know that paper. Cost ~4-5 USD compute + ~2 USD LLM across both cells. Prediction **P4** below.
17. **Fidelity check is the first paid launch.** Set: chip C baseline + top-20 by the 3-workload geomean (mcf, omnetpp, lbm, the columns every cached design shares) + 5 random, and GAP baseline + top-10 of the 40 + 5 random; all at 50M warmup / 50M sim. ~125 simulations at ~7x length, ~2-3 h wall, ~4 USD. **Why:** it is the ruler; a failure invalidates 2,644 cached rows and multiplies every later collection by ~7, so it must be known before anything else is bought. Needs the user's OK to launch.
18. **Structured learn phase: the playbook is fed by design, not by the analyst's luck.** Per training chip (A, B) on the 5-workload training suite: the baseline, every one-knob-away design (25 in space C, minus those over A's budget), the key two-knob pairs (L2 prefetcher x `llc_sets`, `l2_sets` x `llc_sets` under the area cap), and ~40 mixed designs in a fixed random order; ~80 designs x 5 workloads x 2 chips ~= 800 simulations, ~2 h, ~3 USD. `collect.py` already produces exactly this ordering, and the distiller reads every cached row on a problem, so this folds into collection (a) and the analyst-driven learn loop becomes optional. **Why:** the Sep 7 autopsy found ~21 designs per chip and knobs never explored; every knob needs a controlled pair before it can get a rule. Cheapest lever that acts directly on the headline.
19. **Ten seeds in cell 2, five in cell 1, three in cell 3.** Cell 2 with seven arms is ~5,000 simulations, ~13 h, ~20 USD; graph simulations are cheap and Yazdanbakhsh reads variance first. Cells 1 and 2 are the long runs; cell 3 queues behind them.
20. **CACTI, post-hoc only, never a constraint.** Area and energy per cache level for each cell's final best design per arm, in one table. CHIA's CACTI code (`chia/vlsi/sram_cacti/cacti_runner.py`) is a Liberty-file generator for a VLSI flow: it writes every cache as a **direct-mapped SRAM at Sky130 130 nm** and shells out to a CACTI binary the VM does not have (plain `make`, ~2-3 h all in). **Open, user's call:** which technology node to report, and whether to write the CACTI config with the real associativity ourselves (CACTI supports it; CHIA's wrapper does not). **Why:** the judges flagged ChampSim's lack of area/power feedback themselves; "measured with your ruler" is the cheapest signal to Huang and Karandikar.
21. **Miss-count GP target: optional, offline only, lowest priority.** The zero-observation form was already tested offline and lost (the "learned miss counts" row: top-5 at 94.0% vs 96.2% for the knob GP). Untested: the in-loop form, where observations on C accumulate and pooled A/B rows are usable without a chip one-hot because miss counts are chip-invariant. A replay test on cached C rows (`loop.offline`) decides whether it is **adopted or dropped before the mechanism freezes**; if adopted it becomes the GP target of every GP arm including the baselines. Run only after decisions 15-18 are done. Note for the paper either way: in cell 2 the program is unseen, so the only miss-count source is the trace's own miss-ratio curve, which was already the best achievable row (suite Spearman 0.72). **Replay result (`python -m loop.offline replay`, 447 chip C suite designs, 5 draws per k, k = designs observed on C):** knob GP vs miss-count GP + stack, suite Spearman / best real IPC in top-5 as % of 0.7491: k=0 0.60/96.2 vs 0.47/94.0; k=4 0.64/97.2 vs **0.71/99.1**; k=8 0.65/98.6 vs 0.71/98.1; k=12 0.68/99.1 vs 0.72/98.5; k=16 0.70/98.5 vs 0.72/98.4; k=24 0.76/99.5 vs 0.73/99.5. Reading: the miss-count target wins clearly only in the first rounds (4-8 designs), which is where designs-to-90% is decided, and the knob GP catches up by 12-24; the gain is cross-chip only. **Decided (user): not adopted.** The GP target stays log speedup in every arm; the replay stays an offline finding and one paper paragraph (across chips, transferring miss counts instead of speed helps in the first rounds and the plain GP catches up).
22. **Start design stays the untouched chip in every cell (decision 11 stands).** Starting from chip B's best design fitted to C's area was proposed and declined as the primary start, for three reasons: the rules' claims are measured as one-knob effects from the baseline and the claim tests and stall scan are anchored there; B's best already has a prefetcher on and a large LLC, so the rules would have nothing left to say and the contest would move into the interaction tail where rules are weakest; and the trivial first move is already neutralised by scoring designs-to-90/95/99% instead of raw headroom. **Future expansion, once the cells have shown success (user, second review):** repeat the comparison starting from the previous chip's best design fitted to the new chip's budget, alongside the untouched-chip start, and report both. Not a step of the current plan; more value later, not a requirement now.
23. **Rule priors: a bounded shift of the GP mean, never virtual points** (user, with expert input). A categorical claim (`l2_prefetcher = spp_dev`) shifts the GP mean of every candidate carrying that value by measured effect x credibility. A direction claim on a size knob (`llc_sets up`) applies the same flat shift to every feasible candidate above the baseline on that knob - flat, so it never over-promises; how much bigger pays, the simulator says. Decay 1/(1+n), n = real observations already inside the shifted region. The stacked shift on one candidate is capped at 1.5 x the largest single measured effect, or EI builds a "super candidate" and the exploitation trap returns under a nicer name. Only speaking rules shift (condition true, credibility >= 0.5); every rule still bets on every design it speaks about, bet and shift kept separate. **Why:** a mean shift tilts the table without making the GP falsely certain, which is exactly what virtual points did. Optional refinement if it stays small: scale a capacity rule's shift by the workload's own miss-ratio curve. Watch: if the `rules` arm's first two rounds land in one basin again, halve the cap.
24. **New descriptor: movable MPKI, at the LLC and at the L2.** From the trace's miss-ratio curve read against the chip: misses per kilo-instruction that a bigger cache would remove, between the chip's baseline capacity and the largest capacity its area budget affords. Footprint ratio asks how big the book is compared to the shelf; movable MPKI asks how many pages a bigger shelf would let you re-read. **Evidence from the training suite first:** lbm (footprint 26.7 MB, ratio ~5.8) makes every footprint-ratio capacity rule fire, and its measured capacity variance is 0.01; the same failure then shows on pr.urand and bfs.kron (old rules fire, doubling the LLC is -0.7% / -1.2%), and the gate's movable MPKI separates all of them. Conditions: thresholds fitted on training workloads only, never on GAP numbers; the paper states that the descriptor was added after the screening sweep and before any test cell ran; footprint ratio stays available and the threshold fit retires it where it is trivial (the distill prompt names movable MPKI as the capacity descriptor). Bonus: a should-say-no rule becomes writable ("capacity does not pay here and the latency costs you"), a stronger test of the language than silence.



## Decisions taken in the third plan review (expert, after the first cell-2 run; user adopted the list)

Ground: the first cell-2 run (report `REPORT_overnight_v1.md` while it exists; summary in "Start here" item 5). Verdict there: the rule language transferred *where* and *which way*, not *how much*; the playbook was written for a GP (a number per rule) and read by a GP (a tilt), and the plain LLM agent was the reader that could use directions, mechanisms and records and size things itself. So the memory is rewritten for the LLM. Decisions:

**A. Fixes to what exists (all of them).**
25. **Pooled distillation:** one distillation over the A+B tables together with the chip as a column, so a claim is one rule (no cross-chip duplicates that stack their tilts) and thresholds are fitted on ten (chip, workload) points (kills the inverted-sign capacity rule that chip B's 512 KB of headroom produced).
26. **Reject degenerate conditions at admission:** a fitted threshold with every training workload on one side is an "always" rule; flagged as class-agnostic or rejected.
27. **Two-stage re-scoping:** lose on *size* -> shrink the claim (re-bucket); lose on *sign* -> sharpen the condition. A rule whose direction is right is never silenced.
28. **Proper scoring, not midpoint disputes:** every forecaster is scored on its own forecast at fixed thresholds relative to the sibling design - sign right / at least half the claimed gain / at least the full gain - Brier per threshold. Same for the agents.
29. **Bets settled per workload too**, from the per-workload IPC every suite result carries: three times the evidence for free.
30. **Reference and null curve per cell:** a uniform random sample of 300 designs per cell (the held-out reference and the random-search null curve at once). Best-in-run is never the reference again.
31. **`bo_pooled_x`:** the pooled GP plus a mild exploration term, reported next to plain `bo_pooled` (which collapsed with no escape).

**B. Experiment setup.**
32. **Start from the previous chip's best design, fitted to the new chip's budget, in every arm** (reverses decision 22; the memory's cards carry interactions and directions, not baseline steps, so the earlier objection no longer holds). On GAP that design *is* the trap basin: the whole race is escaping it, no free candy, the industrial setting. Every arm counts its first design; `memory`'s recipe design is design 1, not a free start.
33. **Cell 2b:** the other GAP traces on chip C (bfs.road, pr.web, cc). Memory learned in cell 2 is tested there. Replaces the multi-core cell (chip D stays in the code, unused).
34. **Seeds:** 10 on cell 2, 5 on cells 1 and 2b.
35. **Pre-register before the pilot:** P5 `memory` beats `llm_direct` to 95%; P6 more so on 2b than on 2; P7 `handoff` beats `llm_direct` to 95%; P8 `rules_v2` beats `bo`.

**C. Arms:** `random`, `bo`, `bo_pooled`, `bo_pooled_x`, `textbook`, `rules_v2` (fixes 25-27, direction-only tilts until a pair measured on this chip gives the size), `llm_direct`, `handoff` (the plain agent for 6 designs, then a GP on its history), `memory` (below). `full` is dropped. No GP inside `memory`: `handoff` covers the GP tail and keeps the ablation clean: `memory` minus `llm_direct` = the value of memory.

**D. The memory system (the core).** Three shelves and a scoreboard, one JSON file that grows and never deletes.
- **Cases (what happened):** one card per (chip, workload): descriptors, the start design, the best design found, the five largest single-knob effects measured. Ten lines. Retrieved by descriptor distance: the three nearest past cases go into the prompt ("this looks like mcf; mcf liked X").
- **Beliefs (what we think we know):** rules as *mechanism cards*: when (condition in descriptor words), do (knob, direction), because (the mechanism), size (bucket: small < 3%, medium 3-10%, large > 10%), record (held on: ..., failed on: ...). Interactions are cards too ("an aggressive L2 prefetcher makes an LLC prefetcher redundant"). The mechanism is what lets the LLM judge whether a card applies to a class it never saw. Exact magnitudes live only in the record as evidence.
- **Recipe (what to do first):** an ordered decision list ("1. L2 prefetcher by stride class; 2. LLC to the largest affordable if movable MPKI > 1; 3. drop the LLC prefetcher if the L2 one is aggressive; 4. MSHRs down with heavy prefetching ..."). Applied to a new chip's budget and a new workload's descriptors it yields the **starting design**; round 0 = recipe design + one alternative. First recipe: written by the LLM from the cases (best designs and largest effects); later recipes also from the memory arm's own winning trajectories.
- **Scoreboard:** every card bets, per workload, on direction and size bucket before the simulation; Brier-scored. Lose on bucket -> re-bucket; lose on direction -> sharpen the condition. Records persist across chips and classes.
- **How the agent uses it:** round 0 from the recipe; every round the results table, the three nearest cases and the cards whose conditions hold, sorted by record (~1-2k tokens). After a run the agent writes new cards, updates records, appends the case and the trajectory.
- **Test:** cell 2 with memory built from A/B (cross-class: expect directions and the start to help); cell 2b with memory that includes cell 2 (same class: expect a clear win). If memory does not beat the plain agent on 2b, memory is the paper's limitation and the plain agent is the result.

**Cut:** the blind arm (report material later), the multi-core cell, any GP work beyond fixes 25-27.

## Evidence gathered Sep 8 (all offline, zero spend)

**Offline transfer test** (`train on every cached A+B per-workload row, predict every cached C row`; 810 train / 1,834 test rows; harness `loop/offline.py`, reproducible with `python -m loop.offline compare | fair | learned`).

**The morning table was wrong and is corrected below.** A pooled Spearman over all four workloads mostly rewards knowing *which* workload a row is (mcf's log speedups span -0.31..+0.51, xalancbmk's -0.83..+0.02), so it flattered every model. The two metrics that mean something are the rank **inside** one workload and the **suite objective** the loop actually optimises (geomean over the 4 workloads, 447 designs measured on all of them). Same protocol, same rows, corrected columns:

| surrogate (fit on A+B, zero designs seen on C) | pooled Spearman (misleading) | within-workload Spearman | suite Spearman | best real IPC in its top 5 |
|---|---|---|---|---|
| knob columns + soc one-hot (today's `bo_pooled`) | 0.30 | 0.46 | 0.60 | 0.7204 (96.2%) |
| physics features | 0.86 | **0.31** | 0.22 | 0.7184 (95.9%) |
| minimal physics (design-vs-baseline miss deltas) | 0.87 | **0.56** | 0.60 | 0.7188 (96.0%) |
| CPI stack, curve-predicted miss counts | 0.46 | 0.47 | **0.72** | 0.7175 (95.8%) |
| CPI stack, learned miss counts (GP on A+B) | - | 0.45 | 0.47 | 0.7041 (94.0%) |
| *CPI stack, measured miss counts (oracle, not achievable in the loop)* | 0.46 | 0.54 | 0.75 | **0.7491 (100%)** |

Read it as: **G1 fails.** Every achievable model lands in the same 95.8-96.2% band on top-5, i.e. the CPI-stack mean as specified buys nothing over `bo_pooled`'s own GP. The G1 fallback ("minimal-physics GP as the mean") rests on the inflated 0.86; its real within-workload rank is 0.56. What *is* established: with exact miss counts the same 5-coefficient formula puts the true optimum in its top 5 from zero observations on C, so the whole mechanism reduces to one well-posed sub-problem - **predict per-level miss counts accurately** (the learned model reaches 34% relative error at the LLC; the stack multiplies that by ~180 DRAM cycles).

**Miss counts are chip-invariant, IPC is not** (36 designs simulated on both B and C): same design and workload, B -> C changes L1D/L2C/LLC MPKI by 1.8-4.2% (rank corr. 0.99+) while IPC moves 25.8%. So the problem factors: *how many misses* is a (program, design) property learnable on any chip, cheaply and without transfer; *what a miss costs* is a chip property a formula supplies. This is a better factorisation than either v6 extreme and is the next thing to test.

**Where the IPC variation lives** (Tier A full factorial, 81 designs, clean): capacity owns omnetpp (llc_sets 0.76 of variance) and half of mcf (0.41); the L2 prefetcher owns lbm (0.96); interactions are 2.5-23%. The split flips per workload, and the prefetcher's share grows with the chip (lbm l2_pref: A 0.34, B 0.84, C 0.96). This is the paper's motivation figure and the reason a capacity-only mean cannot carry a suite.

**A suite objective forgives model error** that per-workload accuracy condemns: per-workload errors partly cancel in the geomean, and what survives is common-mode, which does not change a ranking. This is why all four models were mis-ranked by the morning's metric.

**Interval model recovered from data:** fitting the stack's overlap fraction as `base + slope x stride_regular_fraction` returns base = 0.000, slope = 0.899 - a program with no regular strides gets no miss overlap at all, which is exactly the model's dependent-miss statement. It lifts mcf's within-workload rank from 0.73 to 0.84.

**Prefetchers hide misses, they do not remove them** (chip C, lbm): `spp_dev` cuts LLC misses 17% and raises IPC 34%; `ip_stride` cuts them 7% and raises IPC 2.7%. A miss-count model cannot see this. A fitted timeliness factor per prefetcher value recovers +0.05 suite Spearman and changes no top pick, so "physics for capacity, rules for policy" survives.

Held-out program (train A+B on 3 programs, predict C on the 4th): no model transfers (mean MAE ~0.17-0.18 for all; xalancbmk anti-correlated). Consistent with the literature (Concorde ISCA 2025 needs 2k-8k samples of a held-out program even with 29 training programs). Hence: physics for chips, not for unseen programs.

**Literature (Sep 8 scan; details in memory `research-sep8-ml-cache`).** Closest prior work: **AgentDSE** (MLArchSys @ ISCA 2026, arXiv 2606.21836): coding agent tunes a ChampSim cache hierarchy on mcf/lbm/omnetpp, ~50 sims vs 1,000 for BO; hypotheses as free text; no transfer, no scoring; its anonymization ablation showed the LLM is a capable black-box optimizer by itself. Also LUMINA (unverified LLM rules), MicroEvo (ICCAD 2026, refuses quantitative claims because of noise), ArchEval (agents' forecasts diverge from results). Learned cross-program transfer (MetaDSE, OneDSE, PerfVec, Concorde) needs 7-29 programs and 10^5 samples; RL for design selection loses to BO/GA at 10x our budget (ArchGym, OneDSE); deep trace surrogates need GPU-days. Mechanistic miss-cost models exist: interval model (TOCS 2009), Van den Steen et al. (ISPASS 2015 / IEEE TC 2016: profile once, predict CPI for any ROB/LLC/MSHR, 4-9% error), Aneto (MICRO 2026: blocking factor fit on 5-7 workloads). Nobody has simulator-settled, Brier-scored, pre-registered architectural claims; framing citations POPPER (ICML 2025), FOREAGENT (ACL 2026), Murphy 1973. Baseline fairness: budget-matched HPO study (2606.21641), MTBO pitfalls (2607.09073).

**Verdicts:** no RL, no deep learning, no learned workload embedding, no LLM-as-surrogate, no multi-fidelity search, no AlphaEvolve handoff, no Merlin arm, no cross-program claim.

## Roadmap with gates (ordered phases, no calendar)

| phase | do | gate |
|---|---|---|
| done | CPI-stack mean built (`loop/cpi_stack.py`) and tested offline (`loop/offline.py`, `loop/surrogates.py`). Reference verified. Offline evidence table corrected. Headline reset to workload-class transfer. | **G1 failed as specified**: suite top-5 reaches 95.8% of 0.7491 with predicted miss counts vs `bo_pooled`'s 96.2%; 100% with oracle counts. Formula sound, miss-count layer is the gap. Stack demoted to an offline finding. |
| done | Both free gates run. `gcc` profiled; GAP format checked from a 12 MB byte-range prefix of `bfs_traces.zip` (Zenodo serves HTTP 206, member is `bfs.kron-29B.champsimtrace.xz`, deflate inside the zip, xz inside that). Admission gate written and validated: `python -m loop.offline admit`. | **G-format: PASS.** 262,144 records parse under our 64-byte `input_instr` layout; `is_branch`/`branch_taken` strictly 0/1, branch rate 0.182, IPs in one hot loop at 0x404190-0x404273. Same naming convention as DPC3. Still to confirm by actually running ChampSim on a full trace. **G-MPKI: `gcc` FAILS** - see decision 9. |
| done | Screened 4 more SPEC17 candidates and 4 GAP traces with `loop.offline admit`, all free. **The zip-index trick:** a GAP kernel's zip is 10 GB but holds 31 separate traces (5 graph inputs x SimPoints) of 24-630 MB each; fetch the zip64 central directory from the last 256 KB, then byte-range exactly the member wanted. So one GAP trace costs ~150-600 MB, not 10 GB. | **G-window: the window is NOT the lever** - `bfs.kron` is capacity-flat at 10M, 30M and 45M alike, because its footprint grows as fast as the window. **What decides it is the graph input, not the kernel or the window.** See decision 9. |
| done | Screening finished (25 traces), suites settled, **G-headroom passed** (0.83 USD, VM stopped). | see decisions 9-11 |
| 1 | **Fidelity check, first paid launch** (decision 17; ~2-3 h of VM or one Mac night, ~4 USD on the VM, user's OK): `COLLECT_SOCS=C_server LOOP_WARMUP=50000000 LOOP_SIM=50000000 python -m loop.collect top 20 <mcf omnetpp lbm>` and `... top 10 <the 3 GAP traces>` (baseline + top + 5 random each, own `_w50M_s50M` tables). Free work alongside: `summarize` fixes (per-cell reference, 90/95/99%, unfinished-arm crash); frozen `bo_pooled` pool; v7 selection (bounded categorical priors, claim tests only on EI stall). | **G0 SPEC: PASSED** (chain `v1`): 26 designs at 50M/50M, Spearman 0.919 against the short runs, 4 of the short top-5 stay in the long top-5, the short best is long rank 3; every design gains a uniform ~+14% IPC at the longer warmup (baseline 0.471 -> 0.520), and the big-LLC advantage survives on all three workloads (mcf ratio 1.40 -> 1.39, omnetpp 1.08 -> 1.10, lbm 1.11 -> 1.15). Short runs rank designs correctly; absolute IPCs are ~14% low. **Shorter is not enough** (same 26 designs, Spearman against 50M/50M): 2M/4M 0.64 (top-5 overlap 2/5), 4M/8M 0.77 (3/5), 5M/10M 0.92 (4/5). So the search length stays 5M/10M; pilots get their speed from fewer arms, rounds and seeds, and from more cores. GAP side: 16 designs at 50M/50M in the `_w50M_s50M` tables (their names did not line up with the short tables in a first check; to be resolved). |
| 2 | Multi-core path (~100 lines): `num_cores` per chip profile in `socs.py`; a trace **list** into `simulate.run_simulation` (ChampSim already requires exactly `NUM_CPUS` traces and reports per-core stats); per-core metric aggregation; area cap counts one L2 per core; mix descriptors (sum capacities, access-weighted rates). Chip D = 4-core CRC-2-shaped, shared LLC. `llm_direct` arm (decision 16). Then, with the user's OK: collection **(a)** = structured learn set (decision 18) on A/B + `fotonik3d`/`cam4` columns for the cached A/B/C designs (~3 + ~6 USD), and **(b)** the held-out reference, ~300 designs x 3 GAP workloads (~4-5 h, ~7 USD). | smoke: one 4-core design runs end to end and its per-core IPCs are sane; each suite has a reference over enough designs to make 90/95/99% meaningful |
| 3 | CHIA path: each arm a Ray task, simulations and builds as CHIA tasks, analyst through `chia.models.vertex`, profiler on (decision 15). Chip D reference rows collected unattended. Optional once phases 1-3 are done: the miss-count replay test (decision 21). | `run_chia.py` runs a Tier C smoke end to end on the VM |
| 4 | Distill **one frozen playbook** from the structured A+B rows on the training suite (mcf, omnetpp, lbm, fotonik3d, cam4); used unchanged in every test cell. Full CHIA smoke of every cell with profiles saved. Miss-count target adopted or dropped here. **Write the pre-registered predictions P1-P4 into the repo, timestamped, before any test cell runs.** | **G-language (free, the first real proof):** against the 40 GAP designs already cached on chip C, the distilled capacity rules fire on `bfs.urand` and stay silent (or say no) on `pr.urand` and `bfs.kron`, and every speaking rule's direction matches the cached one-knob effect. Pass -> the headline stands in miniature before a cell is bought; fail -> the descriptor language is the finding. Also: CHIA smoke passes, `run.py` retired |
| 5 | Fixes from the smokes, the reply-arm and `llm_direct` prompts, cost check against the budget line below. **Mechanism frozen at the end of this phase** (decision 7). | all cells launchable from one command |
| 6 | Through `run_chia.py`: test cell 1 (chip C, SPEC-class, 7 arms x 5 seeds x 24 designs) and cell 2 (chip C, GAP, **7 arms x 10 seeds** x 24), same frozen playbook; then, queued behind them, the multi-core cell (chip D, GAP mixes, `rules` vs `bo_pooled`, 3 seeds). Long unattended runs, launched when the user can. | **G-headline:** in cell 2, `rules`/`full` beat `bo_pooled` and `llm_direct` on designs-to-target, and degrade less than `bo_pooled` from cell 1 to cell 2. Fail -> report which conditions transferred and which did not; that still answers the judges' open question. |
| 7 | 1B-instruction validation of the top ~5 designs per cell per key arm (ArchAgent's cascaded approach: search short, validate long). CACTI area + energy table for the final designs (decision 20, ~2-3 h, post-hoc). | designs that won at 10M still win at 1B |
| 8 | Paper; upstream notes; dataset export; CHIA profiles as the task-graph figure. | submit |
| later | Future expansions once the cells have shown success: the previous-chip's-best start alongside the untouched-chip start (decision 22); CACTI area/energy on more designs; more GAP kernels. | — |

Budget: ~21 USD spent of ~257. Plan above ~85 USD: fidelity ~4, structured learn set + fotonik3d/cam4 columns ~9, held-out + chip D references ~12, cell 1 (7 x 5) ~17, cell 2 (7 x 10) ~20, multi-core cell ~5, 1B validation ~14, `llm_direct` LLM ~2, CACTI ~1. ~55 h of VM time in total, mostly unattended, run in sessions whenever the user can. Compute is not the constraint; correctness is.

## Paid-run design and paper

**Three test cells, one frozen playbook, learned on chips A+B with the training suite (mcf, omnetpp, lbm, fotonik3d, cam4).** The playbook is never re-learned or re-tuned between cells; that is what makes the comparison mean anything.

| cell | chip | workloads | what it isolates | arms x seeds |
|---|---|---|---|---|
| 1 | C (unseen) | mcf, omnetpp, lbm, fotonik3d, cam4 | new chip only | 7 x 5 |
| 2 | C (unseen) | **bfs.urand, pr.urand, bfs.kron** | new chip **and** new workload class - the headline | **7 x 10** |
| 3 | D (unseen, **4-core shared LLC**) | GAP mixes | does it also survive a core-count change | 2 x 3 |

**Arms** (same budget, same baseline start, same seed set): `random`, `bo` (cold GP), `bo_pooled` (frozen A/B pool - the data-transfer baseline), `textbook` (LLM rules written before any result), `rules` (verified playbook as bounded categorical priors, no LLM at test time), `full` (+ analyst), `llm_direct` (the plain agent: the LLM picks every design from the history table alone, no GP, no rules - AgentDSE's setting, decision 16). The `physics` arm is dropped: G1 failed, so it would only be a worse GP. ChampSim is deterministic; variance comes from the optimizer and, for `rules`/`full`, from the analyst (temperature 0.3/0.7) and, for `llm_direct`, from the LLM alone.

**Pre-registered predictions - written into the repo, timestamped, before cell 2 runs.** Applying the mechanism to ourselves; no paper in this space does it, and it is the cheapest credibility we can buy.

- **P1:** `bo_pooled` degrades sharply from cell 1 to cell 2, because its warm-start pool is SPEC rows. (Grounded: our held-out-program offline test showed a learned surrogate does not transfer to an unseen program.)
- **P2:** `rules` degrades markedly less than `bo_pooled`, because its conditions read measurable habits rather than program identity.
- **P3:** `textbook` is mediocre in both cells and degrades least of the three, having never been fitted to anything.
- **P4:** `llm_direct` captures the easy gain fast in both cells (any prefetcher on GAP is +90-186%) but reaches 95/99% of the reference slower than `rules`, and its stated predictions score a worse Brier than the measured rules'. (Grounded: the analyst's hypotheses won 32-54% of their bets in v4/v4.1.)

Report whether each held, with the numbers, and Brier-score ourselves alongside the surrogate, the rules and the analyst.

**Report:** best-so-far vs designs with bootstrap CIs over seeds (the plot Yazdanbakhsh reads first); censored medians to 90/95/99% of each cell's own reference; hit fraction at 12 and 24; final regret; Brier and reliability diagrams per forecaster (Murphy decomposition); **descriptor-range table** (training workloads' descriptor ranges vs GAP's, so the reader sees exactly which conditions are interpolating and which are extrapolating - this is the rigor move that pre-empts "you got lucky"); rule ledger on the held-out class (which rules spoke, win rate, Brier, SPEC vs GAP); corrected offline surrogate table with the CPI-stack oracle row; playbook excerpt with records; 1B-validation table; CACTI area + energy table of the final designs (post-hoc, decision 20).

**Motivation section** = the Sep 7 autopsies in three sentences (exploitation collapse, marginal-vs-interaction effects, simulator-relative thresholds), plus the variance decomposition (capacity owns omnetpp, the prefetcher owns lbm at 0.96, and the split flips per workload).

**What we can claim that ArchAgent cannot,** stated in the intro: (i) knowledge carried from old chips to a new one at all - they start every search from scratch; (ii) hypotheses pre-registered and settled by the simulator with a calibration score - theirs are free text, never scored; (iii) a direct test of extrapolation to an unseen workload class - they name it as an open problem; (iv) validation at competition length via their own cascaded approach. We do **not** compete on IPC: their margins are 1-3% because they design new policies, while we search a knob space, so the numbers are not comparable and we say so.

**Limitations, plainly:** chips differ in provisioning, not in hierarchy shape (block size and level count fixed; only chip D changes core count); footprint theory predicts a *dedicated* cache's miss ratio, so the miss-ratio curve is an approximation on chip D's shared LLC; 2-3 GAP kernels, not the full suite; placeholder SoC profiles, never team-reviewed; L1D miss-ratio prediction ~1.5-2x low; each cell's reference is the best cached design, not a true optimum; cell 3 has 3 seeds and 2 arms; warmup (with the fidelity result); no cross-program claim for the *surrogate* - only for the rule language.

## Repo layout

See `README.md` for the file table. `loop/loop.py` is simulator-agnostic; `loop/champsim_problem.py` is the only ChampSim glue; `loop/chia_nodes.py` + `loop/run_chia.py` run a cell as a CHIA loop (builds and simulations as CHIA tasks, arms as Ray tasks, analyst through `chia.models.vertex`, profiler on); `loop/run.py` holds the cells (`spec`, `gap`, `quad`, `smoke`) and runs them without CHIA; `loop/collect.py` (new) simulates a fixed design list per chip on new traces; `loop/cpi_stack.py` (new) is the CPI-stack mean function (5 global coefficients, chip parameters read from the SoC profile so an unseen chip needs no work); `loop/offline.py` + `loop/surrogates.py` (new) are the zero-cost offline harness and the candidate feature sets; `loop/socs.py` / `configs.py` hold **placeholder** SoC profiles and search spaces; `proposal.tex` is the accepted proposal (its "line size" knob was never in any search space: ChampSim's block size is global); `paper/main.tex` the paper skeleton (must be rewritten to the v7 story). **To build next:** multi-core path (`num_cores` in `socs.py`, trace list into `simulate.run_simulation`, per-core metric aggregation, per-core L2 in the area cap, mix descriptors).

**Code status (third plan review, all offline-verified on the Mac).** Built and smoked: pooled distillation over A+B with the chip as a column (one rule per claim; a size claim whose condition reads movable MPKI the wrong way is rejected; a fitted threshold with every training point on one side is flagged `always`); bets as three fixed questions per claim a design carries (sign / half / full, relative to the sibling), answered by the rule, the surrogate and the reply, settled on the suite and, with a measured sibling, on every workload; credibility from sign bets only; two-stage re-scoping (sign losses sharpen the condition, size losses halve the claim down to 1%); direction-only tilts of 5% until a pair measured on this chip gives the size; `bo_pooled_x` (EI + 0.5 std); every arm starts from chip B's best design on the training suite fitted to the cell's chip (`run.previous_best_start`); arms `handoff` (agent 3 rounds, then GP on its history) and `memory`; `full` dropped; the memory system in `loop/memory.py` (cases from the tables, mechanism cards and recipe written by the LLM, retrieval by descriptor distance, cards bet on direction and bucket per workload, re-bucket / sharpen, write-back to a per-run file, `python -m loop.memory consolidate`); `loop.collect uniform` (reference + null curve) and `loop.collect fetch_gap` (one Zenodo zip member by byte range); cells `spec` / `gap` / `gap2b` / `smoke` / `learn`; pre-registration in `PREREGISTRATION.md`; the pilot chain in `cluster/chain.sh`. Smokes on the Mac: a 12-round `rules` dry run from B's best on cached rows (sign bets won 16-7 / 16-8 / 17-5 while size bets lost and the claims were re-bucketed to +1.6 / +2.8 / +2.0%); pooled distillation on A+B over three workloads gave 6 rules, one per claim, the LLC rule on movable MPKI and silent on the capacity-flat GAP workloads (P9 in miniature); the memory built from A+B (6 cases, 10 cards, 6-step recipe) and the agent ran two rounds end to end with card bets and write-back. **Not built:** cell 2b's third trace (cc) and the fetch of its traces onto the VM; CACTI.

## Setup (not in repo)

```bash
git clone --depth 1 https://github.com/ChampSim/ChampSim.git champsim
cd champsim && git submodule update --init && ./vcpkg/bootstrap-vcpkg.sh && ./vcpkg/vcpkg install
./config.sh champsim_config.json && make -j8 && cd ..
git clone --depth 1 https://github.com/ucb-bar/chia.git chia
uv venv --python 3.10 .venv && uv pip install -p .venv/bin/python -e ./chia google-genai
mkdir traces && curl -o traces/605.mcf_s-665B.champsimtrace.xz \
  https://dpc3.compas.cs.stonybrook.edu/champsim-traces/speccpu/605.mcf_s-665B.champsimtrace.xz
```

**GAP traces (the held-out workload class), on the VM only** — Zenodo record 20043527 (BSC/UPC/TAMU, May 2026; 46.4 GB for six kernels, so fetch only bfs/pr/cc, ~10 GB each). Cite *"Characterizing the impact of last-level cache replacement policies on big-data workloads"*, IISWC 2020. Verify the 64-byte `input_instr` record format before relying on them.

GCP: project `project-c23a6080-f5d0-4871-9cb`, ADC auth, `aiplatform.googleapis.com` enabled; project-wide cap 32 vCPUs (`CPUS_ALL_REGIONS`; the regional C2D quota of 100 is not binding); VM `champsim-1` (c2d-standard-32, europe-west4-a, ~1.5 USD/h) is STOPPED; recipe in `cluster/README.md`. `ANALYST_MODEL=gemini-2.5-pro` is the only Pro the project can call (default `gemini-2.5-flash`). 14 candidate SPEC17 traces for later expansion are listed in the DPC3 directory (gcc, x264, deepsjeng, leela, bwaves, fotonik3d, roms, xz, cactuBSSN, wrf, imagick, nab, perlbench, exchange2); profiling costs ~2 s per trace, downloading ~200 MB each.

## Datasets in the repo

- **Cached simulation tables** (`results/tierC_<chip>_<trace>.json`, one per chip and trace; `_w50M_s50M` etc. for long runs): per-workload rows keyed by design name; A ~430, B ~380, C ~1,830 rows over mcf, lbm, omnetpp, xalancbmk, plus 40 designs x 3 GAP workloads on C. The simulation cache every command reads and appends to, atomic-written, one lock per file; identical designs give identical rows on the Mac and on the VM (verified), so tables from both machines merge by name (`loop.collect merge`).
- **Dense 81-design sweeps** `results/sweep_*.json` (3 chips x 3 traces): the variance decomposition in the motivation section came from these.
- **Workload profiles** `results/profile_*.json` (25 traces): the chip-independent half of every rule condition; built once per trace, no simulator.

## History (condensed)

- **Earlier runs, superseded and deleted from the repo (numbers kept here only):** single-workload runs on a 46k-design space, median over 9 runs, designs to 90% of gain: full 8, rules 6, bo 15, bo_pooled 15, random 19 (chip-relative conditions). Suite runs on chip C, seed 0, Pro analyst, two threshold variants: full 9 / 8, bo_pooled 6 / >24, rules 11 / >24, bo >24 / 18; the `rules` collapse came from a claim-test flood and virtual-point priors pulling EI into one basin - the failures that decisions 23 and the stall-gated claim tests answer. Calibration on shared bets then: rules Brier 0.21-0.27, surrogate 0.29-0.31, analyst 0.28-0.29.
- **v2 (Sep 3):** disagreement selection, flat-confidence rules, claims never settled. **v3 (Sep 4):** controlled-pair verification, every bet counts; found that a rule's gain is a marginal effect at the baseline. **v4/v4.1 (Sep 5):** EI with rule/hypothesis priors, owed claim tests, direction claims, analyst credibility; Tier B result. **Sep 7 autopsies (Tier C):** exploitation collapse from priors; thin playbook; untestable capacity rules; analyst deadlock at credibility 0.5; sign contradictions; area coupling (bigger L2 makes the decisive LLC step infeasible); chip-relative thresholds silenced the good rules. Fixes: stall scan, right of reply, adjacent-pair verification, `direction` field, rewritten prompts, Pro analyst, **v5 trace-profile descriptors**, **v5.1 thresholds fitted from per-workload evidence**. **Sep 8:** honest review (one seed, pooled BO ties, CHIA integration thin, SoC profiles unreviewed); offline test shows physics features carry rank 0.86 across chips but nothing transfers to an unseen program from 3 programs; literature scan; external review adds fixed reference, frozen pool, fidelity check, CPI-stack as GP mean; decisions above.

## Problem scope: real problems are hard problems

Per cache level: size, associativity, replacement, prefetcher, MSHR/queue depths (block size is global in ChampSim and not a knob); plus coherence, interconnect, DRAM. Knobs interact through the area constraint. One detailed simulation costs hours per design per workload in industry; teams evaluate tens to low hundreds of designs per cycle across 20-50 workloads. Every new SoC starts from the previous one: chips A, B -> unseen C *is* the industrial loop. Hardness = wide space, sparse near-optimal band, large headroom, interacting knobs; Tier A (54 feasible) is a unit test, Tier C (415k designs, latency coupled to size, MSHRs, 4-workload suite, chip C baseline 0.599, best cached 0.7491) is the target. Baselines must scale with the problem, never be weakened.

## Working rules

- Budget ~257 EUR total GCP credits; ~21 USD spent. Every launch needs the user's explicit OK with cost and time. Nothing runs on the Mac (reserved). Stop the VM when idle.
- Code style: explicit, procedural, junior-readable; no clever one-liners; no flags for dead features, remove them. Commit messages informal, no authorship trailers.
- Domain decisions (search space, SoC profiles, descriptors, arms, prompt content, objective) belong to the user and their PhD teammates: surface options with tradeoffs, recommend, let them pick.
- Offline first: anything testable on the cached rows is tested there before a simulation is bought.

## How to read results and run things

`python -m loop.summarize <report.json> [more.json] [--reference X]` (designs to 90/95/99% of the fixed reference, hit fraction, final best, AUC, calibration per forecaster; works on a running report); `python -m loop.run_chia local <cell> <tag> [playbook]` or `python -m loop.run <cell> <tag> [playbook]` (cells `spec`, `gap`, `quad`, `smoke`; env `SEEDS`, `FIRST_SEED`, `ANALYST_MODEL`); `python -m loop.collect structured|top <n> <traces...>` to simulate a fixed design list (learn set / fidelity and validation with `LOOP_WARMUP` `LOOP_SIM`), `python -m loop.collect merge <dir>` to union another machine's tables, `python -m loop.offline language <playbook>` for gate G-language; `python -m loop.trace_profile <trace.xz>` for one profile.

**Operational lessons.** Launch long runs detached on the VM (`setsid nohup ... &`); kill by process group, never `pkill -f` a broad pattern (it killed 4 live runs on Sep 5); result tables are the shared simulation cache, atomic-written, one lock per file; suites evaluate one design on 4 workloads at once; the process pool forks, so editing files under a running job is safe but the job keeps the old code. Measured throughput on c2d-standard-32: ~380 simulations/hour including builds (not 1 CPU-minute each).
